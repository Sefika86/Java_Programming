package day22_arrayList;

import java.util.ArrayList;

public class ArrayListIntro {
    public static void main(String[] args) {

         //difference between Arrays & ArrayList


        int[] array = new int[5];

        // this is an array , size is fixed to 5, this is a huge disadvantage
        // for adjusting the arrays we had end up with creating new array objects.
        //it supports both primitive & non-primitives

        ArrayList  <Integer> list = new ArrayList < > ();
   //              wrapper c.                     oppt.

        //this is an ArrayList, size is adjustable, this is the advantage
        // now this arrayList is empty, which means size is 0.
        // we have Array List methods to adjust the Array Lists, we can add elements, remove elements...just without
        // creating another arraylist







    }
}
